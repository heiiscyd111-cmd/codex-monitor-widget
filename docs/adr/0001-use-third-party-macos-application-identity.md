# Use a third-party macOS application identity

The macOS package uses the bundle identifier `io.github.asusfx80.codex-monitor-widget` rather than the existing Windows AppUserModelId under `com.openai`. The stable identifier reflects that this is a community-maintained project and avoids claiming OpenAI's namespace, while the Windows identifier remains unchanged for release compatibility.
